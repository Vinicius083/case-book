{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Pacotes internos empacotados com tsup: resolução de bundler, sem decorators.
    "lib": ["ES2023"],
    "types": ["node"],
    "noEmit": true
  }
}
